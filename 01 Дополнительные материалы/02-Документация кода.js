/**
 * Функция преобразует строку с временем в минуты
 * @param {string} time время в виде строки, например, "02:00". Может быть в диапазоне от "00:00" до "23:59".
 * @return {number} вернет 120.
 */
function timeToMinute(time){

}

timeToMinute()